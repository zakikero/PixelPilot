[project]
name = "pixelpilot"
version = "0.1.0"
description = "Offline local-first PixelPilot bot scaffold"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
